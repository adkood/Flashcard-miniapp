import { CardDto } from "../dtos/Card.dto"

export const createDeckForUser = (userId: string) => {

    try {

    } catch(error) {

    }

}

export const createCardForDeck = (data: CardDto, userId: string) => {
    try {

    } catch(error) {
        
    }
}